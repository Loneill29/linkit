<% if favorite = current_user.favorite_for(post) %>
  <%= link_to [post, favorite], class: 'btn btn-danger favorite', method: :delete do %>
    <i class="glyphicon glyphicon-star-empty"> </i>&nbsp; Unfavorite
  <% end %>
<% else %>
  <%= link_to [post, Favorite.new], class: 'btn btn-primary favorite', method: :post do %>
    <i class="glyphicon glyphicon-star"> </i>&nbsp; Favorite
  <% end %>
<% end %>
