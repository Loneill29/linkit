<br>
<%= form_for [question, answer] do |f| %>
  <div class="form-group">
    <%= f.label :body, class: 'sr-only' %>
    <%= f.text_field :body, class: 'form-control', placeholder: "Add a new answer" %>
  </div>
  <%= f.submit "Save", class: 'btn btn-primary' %>
<% end %>
